---
description: US English for prose and comments that are committed into this repository (README, docs, code, rules bodies). Does not govern the agent chat language. Copy into .agents/rules/ when scaffolding; use alwaysApply true when this folder is its own workspace.
alwaysApply: true
---

# Scope

All work here follows the **Requirements** below. In **aap-workbench**, this file lives at **`.agents/rules/all--english-content.mdc`** with `alwaysApply: true` (workbench and **`artifacts/`**). Each **`aap-demo-*`** artifact should also ship a copy under its own **`.agents/rules/`** (`alwaysApply: true`, copied from **`.agents/rules/`** in the workbench root) so the standard still applies when only that folder is opened as the workspace root.

This rule governs **text that belongs in the repository** (or that you intend to commit), not the language of a normal **conversation with the agent** (Cursor Chat, OpenCode, etc.).

# Requirements

Use **US English** for repository-bound prose and explanations (the bullets below), not for ephemeral chat dialogue unless the user asks for English there too.

- **`README.md`**, everything under **`docs/`**, **comments** in YAML, Ansible, and other code, **`task` and play `name`** strings, **variable titles** and human-readable descriptions in **`group_vars/`**, **`vars/`**, role **`defaults/`**, and **commit messages**.
- When this repository includes the workbench layout: skill under **`.agents/skills/ansible-demo/`**, knowledge in **`knowledge/`**, comments in **`scripts/`**, and **`.agents/rules/*.mdc`** bodies you add or edit.

# Out of scope

- **Agent chat (Cursor Chat, OpenCode, and similar assistant UIs)**: respond in the **same language the user writes in** for that thread, unless they explicitly request another language. This rule does **not** require English in chat.
- **Personal notes or scratch text** that never ships in the repo are outside this rule (still avoid putting secrets in chat logs).

# Exceptions

- **Technical identifiers** and **verbatim platform strings** (module names, API fields, exact UI labels in reproductions, log lines) stay as required by the upstream system.
- **Third-party blocks** shipped unchanged (for example license headers) are not translated for the sake of this rule.

**Workbench sync**: The same body lives in **`.agents/rules/all--english-content.mdc`** at the workbench root (`alwaysApply: true`, no `globs`).
