---
# Host-specific network identity for web-01.
# MAC values are populated from the platform inventory; change the
# CHANGE_ME_* placeholders during the demo.
# The baseline group_vars/all/networking.yml provides name, type, and state.

network_host_overlay:
  primary_dhcp:
    mac: "02:a4:1a:55:68:29"

  static_internal:
    mac: "02:a4:1a:55:68:2a"
    ip:
      address:
        - 192.168.152.11/24
